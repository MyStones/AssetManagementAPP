@model IEnumerable<WebAsset.Models.Asset>

@{
    ViewBag.Title = "ViewAsset";
}

<head>
    @Styles.Render("~/css/GetAsset.css")
    <style>
        th {
            font-family: serif;
            font-weight: 900;
        }
    </style>
</head>

<body>
    @using (Html.BeginForm("GetAsset", "Asset", FormMethod.Get))
    {
        <div class="container">
            <div class="row">
                <div class="col-md-offset-1 col-md-10">
                    <div class="panel">
                        <div class="panel-heading">
                            <div class="row">
                                <div class="col col-sm-3 col-xs-12">
                                    <h4 class="title" style="font-family:'Times New Roman';font-weight:1000;font-size:40px">Asset <span>List</span></h4>
                                </div>
                                <div class="col-sm-9 col-xs-12 text-right">
                                    <div class="btn_group">
                                        <input type="text" class="form-control" placeholder="Search">

                                        <button class="btn btn-primary btn-lg" title="SortByAssetName"><i class="fa-solid fa-arrow-down-arrow-up"></i>@Html.ActionLink("SortName", "GetAsset", new { SortOrder = ViewBag.AssetName }, new { style = "color:white;font-weight:900;" })</button>
                                        <button class="btn btn-primary btn-lg" title="SortByAssetType"><i class="fa-solid fa-arrow-down-arrow-up"></i> @Html.ActionLink("SortType ", "GetAsset", new { SortOrder = ViewBag.AssetType }, new { style = "color:white;font-weight:900;" })</button>
                                        <button class="btn btn-primary btn-lg" title="Menu">@Html.ActionLink("Home", "Index", new { }, new { style = "color:white;font-weight:900;" })</button>
                                        @*<button class="btn btn-default"><a href="@Url.Action("Index", "Asset")"><i class="fa-sharp fa-solid fa-eye"></i></a></button>*@
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="panel-body table-responsive">
                            <table class="table">
                                <thead>
                                    <tr>

                                        <th style=" color: #ffffff; font-size: 20px; padding: 10px 12px; vertical-align: middle;">Asset Name</th>
                                        <th  style="color: #ffffff; font-size: 20px; padding: 10px 12px; vertical-align: middle; ">Asset Type</th>
                                        <th  style="color: #ffffff; font-size: 20px; padding: 10px 12px; vertical-align: middle; ">Asset SKU</th>
                                        <th  style="color: #ffffff; font-size: 20px; padding: 10px 12px; vertical-align: middle; ">Price</th>
                                        <th  style="color: #ffffff; font-size: 20px; padding: 10px 12px; vertical-align: middle; ">Count</th>
                                        <th  style="color: #ffffff; font-size: 20px; padding: 10px 12px; vertical-align: middle; ">Description</th>
                                        <th  style="color: #ffffff; font-size: 20px; padding: 10px 12px; vertical-align: middle; ">Vendor</th>
                                        <th  style="color: #ffffff; font-size: 20px; padding: 10px 12px; vertical-align: middle; ">Created On</th>
                                        <th  style="color: #ffffff; font-size: 20px; padding: 10px 12px; vertical-align: middle; ">Updated On</th>

                                    </tr>
                                </thead>


                                @foreach (var item in Model)
                                {

                            <tr>

                                <td> @Html.DisplayFor(modelItem => item.AssetName)</td>
                                <td>@Html.DisplayFor(modelItem => item.AssetType)</td>
                                <td>@Html.DisplayFor(modelItem => item.AssetSku)</td>
                                <td>@Html.DisplayFor(modelItem => item.Price)</td>
                                <td> @Html.DisplayFor(modelItem => item.CountAsset)</td>
                                <td> @Html.DisplayFor(modelItem => item.Description)</td>
                                <td>@Html.DisplayFor(modelItem => item.Vendor)</td>
                                <td>@Html.DisplayFor(modelItem => item.CreatedOn)</td>
                                <td>@Html.DisplayFor(modelItem => item.UpdatedOn)</td>
                            </tr>
                                    <br />

                                }

                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    }


</body>
